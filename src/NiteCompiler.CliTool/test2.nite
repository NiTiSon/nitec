#!bin/nite

module test;

public simple_func(x: i32) -> i32 {
	return x += x = (((((!~+(-3) * 2 / 3 % 4) + 5) - 6)
		<< 1) >> 2 >>> 3)
		& 7 ^ 8 | 9;
}

module std::numerics;

public type SInt32;
public type Float32;

module std;

public type Boolean;