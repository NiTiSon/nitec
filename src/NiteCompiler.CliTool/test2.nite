#!bin/nite

module test;

public simple_func(x: i32) -> i32 {
	loop {
		x = x + 1;

		if (x == 50) return 1;
	}
	return x;
	// bug: no phi nodes there
}

module std::numerics;

public type SInt32;
public type Float32;

module std;

public type Boolean;